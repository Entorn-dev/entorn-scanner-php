name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
      - run: dotnet restore --locked-mode --runtime linux-x64
      - run: dotnet test --no-restore --configuration Release
      - name: Verify reproducible archive
        run: |
          scripts/package-linux-x64.sh 2.0.0
          cp artifacts/entorn-scanner-php-linux-x64-2.0.0.tar.gz /tmp/first.tar.gz
          scripts/package-linux-x64.sh 2.0.0
          cmp /tmp/first.tar.gz artifacts/entorn-scanner-php-linux-x64-2.0.0.tar.gz
