# Contributing

Contributions are welcome through GitHub pull requests. Every commit must include a Developer Certificate of Origin 1.1 sign-off created with `git commit -s`.

Run the locked Release test suite before opening a pull request. Scanner changes must remain deterministic, bounded, evidence-backed, and must not execute analyzed repository code or assume network access.
